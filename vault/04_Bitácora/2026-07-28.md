---
fecha: 2026-07-28
tags: [bitacora]
---

# Bitácora — 2026-07-28

## Qué se hizo

- Rediseño estético integral de los Excel generados por instrucción del cliente:
  hoja A3 horizontal (fitToWidth = 1), Times New Roman 28 en todo el contenido,
  encabezados de tabla verde claro DML `C6E0B4` con texto verde oscuro `375623`
  (azul `1F4E78` eliminado), exactamente una fila en blanco entre bloques,
  alturas de fila ≥ 38 pt y anchos de columna recalibrados.
- Creado `scripts/estilos_excel.py`: módulo ÚNICO de formato corporativo
  (paleta, fuentes, geometría A:O, ajuste de alturas, `configurar_pagina_a3`).
  Elimina el código de estilos triplicado en los 3 generadores, causa raíz de
  que el formato "se perdiera" en cada regeneración.
- Refactorizados `generar_excel.py`, `scripts/generar_dts.py` y
  `scripts/generar_lis.py` para importar el módulo; flag `_tras_blanco` en
  HojaDTS/HojaLIS para garantizar una sola fila en blanco; reserva de imágenes
  de DTS-001 recalculada (9 filas × 38 pt por imagen).
- Creado `scripts/verificar_formato_excel.py` (read-back openpyxl).
- Emitidos los 7 entregables con `scripts/emitir.py` (INF-001/002 con 0 errores
  LaTeX). La primera corrida falló con WinError 1224 (archivo de `Emisiones/`
  abierto por el usuario); se completó al cerrarlo.
- Ver [[2026-07-28_formato-excel-a3-tnr28-verde]].

## Sesión 2 (tarde) — geometría 35/50 y REV0 única

- Geometría Excel fijada por instrucción del cliente: ancho de columna 35 y
  alto de fila 50 en TODAS las hojas de contenido de los 5 libros; portadas y
  encabezado corporativo (filas 1-7) conservan la plantilla. Cambio en
  `scripts/estilos_excel.py` (`ANCHO_COLUMNAS = [35]*15`, `ALTURA_LINEA = 50`,
  `ALTURA_MINIMA = 50`); la reserva de imágenes de DTS-001 ya era dinámica
  (7 filas por imagen con 50 pt).
- **REV0 única (decisión del cliente):** como no se ha entregado nada al
  cliente, todos los documentos quedan en Revisión CERO (0) conforme a GP-N-09.
  Eliminadas todas las referencias a REV1/REV2 de las fuentes de entregables:
  hojas de datos HD-VENT/FILT-001 (notas de revisión y metadatos),
  `listado_equipos.md`, `informe_investigacion.md`, INF-001 (metadatos,
  `00_hojafirmas.tex`, `00_portada.tex`, `07_bases_disenio.tex`, sección Nota
  de Revisión eliminada), INF-002 (metadatos, secciones 10/11),
  `datos_proyecto.tex` (macros \descRevUno… eliminadas), `pdf_dts001.py`,
  `emitir.py` (comentarios), `codificacion.md`, `docs/index.html`,
  `bases_diseno.yaml`, `generar_img_dts001.py`.
- Re-emisión completa: 7 entregables + PDF alternativo DTS-001. Verificación
  automática sobre `Emisiones/`: 0 coincidencias REV1/REV2 en xlsx y pdf,
  EXCEPTO `Emisiones/2.0 HV-MEMORIAS DE CALCULO/P2437-HV-CAL-001.pdf`, que es
  una exportación manual del 2026-07-27 (obsoleta: contiene "(REV1)" y la
  geometría anterior). Pendiente decidir si se retira o se re-exporta desde
  Excel local. Igualmente `P2437-HV-DTS-002.pdf` y `P2437-HV-LIS-001.pdf` son
  exportaciones manuales potencialmente obsoletas en geometría.
- Ver [[2026-07-28_rev0-unica-revision-cero]].

## Verificación

- `python scripts/verificar_formato_excel.py`: los 5 libros (CAL-001,
  DTS-001/002/003, LIS-001) pasan — A3 landscape, TNR 28, relleno `C6E0B4`,
  sin azul, alturas ≥ 38 pt, sin dobles filas vacías.
- Sin cambios en valores numéricos ni fórmulas (solo formato); no aplica
  validación dimensional.
- Revisión visual final en Excel pendiente por el usuario (no hay Excel local).

## Pendientes

- Revisión visual de los 4 Excel emitidos; ajuste fino solo en
  `scripts/estilos_excel.py` si hace falta.
- Considerar llevar la nueva paleta/formato al PDF alternativo de DTS-001
  (`scripts/pdf_dts001.py`, hoy conserva el formato azul anterior).
- Bloqueos de siempre: selección del ventilador mural Ø560 mm con fabricante,
  dimensiones de cubierta intemperie con submittal, confirmación Durafil ES3,
  ensayo de balanceo.
